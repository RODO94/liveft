{
  "name": "liveft",
  "private": true,
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "lint": "pnpm --filter client lint && pnpm --filter server lint",
    "typecheck": "pnpm --filter server typecheck",
    "build": "pnpm --filter server build:netlify && pnpm --filter client build"
  }
}
