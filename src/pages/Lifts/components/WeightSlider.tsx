export default function WeightSlider() {
  /**
   * This component should take in the max weight
   * And use a slider to select a percentage
   * Which displays the weight at that percentage of max
   */

  return <></>;
}
