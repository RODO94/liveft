[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter client build"
  publish = "client/dist"

# SPA fallback — serve index.html for all client routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
